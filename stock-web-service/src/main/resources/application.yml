server:
  port: 8084
      
      
# Eureka client
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}      
  
spring:
  application:
      name: stock-web-service